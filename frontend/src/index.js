import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />,document.getElementById('root'));

// O <App /> é um componente, que é escrito como uma tag
// A função ReactDOM.render() está renderizando o HTML retornado pelo componenete App no componenete de ID 'root'
